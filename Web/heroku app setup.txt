1. creat app folder
2. put code files there + requirments.txt + Procfile
3. git init
4. git add .
5. git commit -m "commit message"
6. heruko login
7. heroku create --> get the random app name form here and the url fot the app
8. heroku git:remote -a heroku-app-name
9. git push heroku master

u can check remote but using `git remote -v`